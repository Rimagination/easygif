interface:
  display_name: "EasyGIF"
  short_description: "Animate, validate, and package visual assets"
  default_prompt: "First resolve or generate one canonical reference and organize a compact production card, then animate or package the requested visual asset, infer a natural motion when needed, preserve the reference, choose the safest route, and produce validated final files or a platform-ready sticker package."
